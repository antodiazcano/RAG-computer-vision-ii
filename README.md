# TODO

1. Hacer que lea la db desde el frontend

2. Que quede más bonito cuando hace el RAG el chatbot, sale ahroa mismo lo de preview y no aparecen los scores

3. Include equations (for example using the LaTeX files .tex instead of pdf).

4. Make it available for anyone? May be introducing API_KEY in the frontend? And public vector db?

5. Allow more models for the chatbot

6. Añadir que se hagan embeddings de las imágenes, no solo del texto
